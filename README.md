# Divers

## Design tokens Cerba — ⚠️ en mode repli

`design-tokens.md` documente l'identité visuelle Cerba, mais **aucune valeur n'a pu être
extraite de la source** : l'accès réseau sortant est bloqué dans l'environnement d'exécution
(refus `403` sur le tunnel `CONNECT`, pour tous les hôtes — voir §2 du fichier).

État actuel :

- Couleurs et polices = **valeurs de repli**, pas les valeurs Cerba réelles.
- Couleur secondaire, rayons, échelle typographique, densité d'espacement = **non extraits**.
- Logo officiel = **non téléchargé**. `assets/logo-cerba-placeholder.svg` est un emplacement
  neutre marqué « logo Cerba ». **Le logo n'a pas été redessiné.**
- Motif « coup de pinceau » = **non récupéré**, et non redessiné.
- Motif de mise en gras partielle des titres = **sourcé et applicable** (§10).

À faire dès qu'un accès à `www.lab-cerba.com` est disponible : suivre la checklist §11 de
`design-tokens.md` et retirer les mentions ⚠️/⛔ des seules lignes réellement remplacées.
